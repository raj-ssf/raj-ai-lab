# External Secrets Operator is installed via Helm in Terraform
# (see terraform/main.tf — helm_release.external_secrets)
# This file only creates the namespace for clarity in GitOps.
apiVersion: v1
kind: Namespace
metadata:
  name: external-secrets
