FROM python:3.12-slim
RUN pip install --no-cache-dir fastapi uvicorn sentence-transformers
COPY 05-embedding-code.py /app/main.py
# No model baked in — downloaded at startup from HuggingFace
# and cached to PVC at /app/model-cache
CMD ["python", "/app/main.py"]
